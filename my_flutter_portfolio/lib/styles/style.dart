import 'package:flutter/material.dart';

import '../constants/colors.dart';

final BoxDecoration kHeaderDecoration = BoxDecoration(
  gradient: const LinearGradient(colors: [Colors.transparent, bgLight1]),
  borderRadius: BorderRadius.circular(100.0),
);
